/**
 * Home feature — the landing screen. A dynamic hero card (knows what you want to
 * do next from your game log) sits above the flatground robot roster. Voice mode
 * is a first-class action from the hero, not a hidden door three taps deep.
 */
export { default as HomeScreen } from './HomeScreen';
